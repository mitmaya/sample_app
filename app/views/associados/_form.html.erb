<%= form_for(@associado) do |f| %>
  <% if @associado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@associado.errors.count, "error") %> prohibited this associado from being saved:</h2>

      <ul>
      <% @associado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
